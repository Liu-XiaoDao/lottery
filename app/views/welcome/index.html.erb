
<div class="welcome-box">
  <h2 class="title"><%= @web_config["web_name"] %></h2>
  <% if @user.present? %>
    <!-- <p style="text-align: center;">
      <img src="<%#= @user.avatar_url %>" style="width: 100px; border: 1px #eee solid; border-radius: 5px;" />
    </p> -->
    <h3>
      热烈欢迎 "<%=@user.name%>" 的加入
      <br>
      有任何问题可以联系你的小组长(<span style="color:red;"><%=@user.user_list.try(:leader_name)%></span>)，或者工会成员
      <br>
      系统id(<span style="color:red;"><%= @user.id %></span>, 已通过短信发送)用于查询<a href="/users/research?utf8=✓&post[username]=<%=@user.name%>&post[user_id]=<%=@user.id%>">报名结果</a>
    </h3>
  <% end %>
  <p>当前参加：<%=@count%>人</p>
  <p>
    报名结束请联系小组长&nbsp;&nbsp;/&nbsp;&nbsp;<a href="/research">查询入口</a>
    <br/>
    <a href="/ygfzb.xlsx" target="_blank">员工分组表</a>
  <!-- 亲，你来晚了，等着小组长统计吧 -->
  </p>
  <p>
    活动日期：<%= @web_config["time"] %> <br />
    活动地点：<%= @web_config["address"] %><br />
    <a href="/activity_introduction">点击查看活动详情</a>
  </p>
</div>
