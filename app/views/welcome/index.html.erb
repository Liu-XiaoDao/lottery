
<div class="welcome-box">
  <h2 class="title">2018年会官方网站</h2>
  <% if @user.present? %>
    <p style="text-align: center;">
      <img src="<%= @user.avatar_url %>" style="width: 100px; border: 1px #eee solid; border-radius: 5px;" />
    </p>
    <h3>
      系统欢迎 "<%=@user.name%>" 的加入,
      <% if @user.is_active %> 
        你的信息已被激活生效
      <% else %> 
        请在会场签到激活你的信息
      <% end %>
    </h3>
  <% end %>
  <p>当前 激活 / 参加：<%=@count_active%>/<%=@count%>人</p>
  <p>
    <a href="/register">报名入口</a>
  </p>
  <p>
    活动日期：2017年1月21日 15：00--21:00 <br />
    活动地点：万金酒店
  </p>
</div>
