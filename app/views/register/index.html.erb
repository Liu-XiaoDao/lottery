<% flash.each do |name, msg| %>
  <div class="uk-alert uk-alert-<%= name %>" data-uk-alert="">
    <a href="" class="uk-alert-close uk-close"></a>
    <p><%= msg %></p>
  </div>
<% end %>
<div class="register-box">
  <h1 class="title">请填写以下信息</h1>
  <%= form_for :register, url: register_index_path, method: :post, html: { class: "uk-form uk-form-stacked" } do |f| %>

    <div class="uk-form-row">
      <label class="uk-form-label" for="">开户姓名</label>
      <div class="uk-form-controls">
        <input type="text" id="username" value="" name="post[username]" class="uk-width-1-1" placeholder="请输入开户姓名">
      </div>
    </div>

    <div class="uk-form-row">
      <label class="uk-form-label" for="">手机号码</label>
      <div class="uk-form-controls">
        <input type="number" id="phone" value="" name="post[phone]" class="uk-width-1-1" placeholder="请输入你的手机号码">
      </div>
    </div>

    <div class="uk-form-row">
      <label class="uk-form-label" for="">活动类型</label>
      <div class="uk-form-controls">
         <span class="id-pre uk-width-1-1">
           <select id="pre" name="post[is_attendance]">
             <option value="两个都参加">两个都参加</option>
             <option value="每月1G流量">每月1G流量</option>
             <option value="每月100分钟通话">每月100分钟通话</option>
          </select>
         </span>
      </div>
    </div>

    <div class="uk-form-row families-div">
    </div>

     <div class="uk-form-row">
      <button class="uk-button" id="btn_add" type="button">+</button>
      <button class="uk-button" id="btn_remove" type="button">-</button>
      </label>
    </div>

    <div class="uk-form-row">
      <label class="uk-form-label" for="">
      <button class="uk-button uk-button-primary" id="btn_submit" type="submit">提交，注册参加活动</button>
      </label>
    </div>
  <% end %>
  <h3 class="tm-article-subtitle" style="color:red;">注意事项</h3>
  <ul class="uk-list">
    <li>1.一年1元，包含每月1G全国流量，不可以共享，没有任何保底限制，流量不能结转下个月。</li>
    <li>2.一年1元，每月移动拨打移动100分钟通话，需要有保底28元，消费不足将予以补收。</li>
    <li>3.如果一年时间内取消以上两个活动，或者携号转网需要交违约金</li>
    <li>（1）  流量：假如我已经使用5个月不想用需要缴纳金额是：已经使用月份*20就是20*5=100元。</li>
    <li>（2）  移动通话：假如我已经使用5个月不想用需要缴纳金额是：剩余月份保底金额总额*20%就是7*28*20%=39.2元。</li>
    <li>4.公司全体员工有移动号码都可以参加（包括家属），需要在钉钉链接中写清楚参加内容，与开户人姓名与移动电话号</li>
    <li>5.报名截止时间明天中午11点，开始时间等我们登记信息完成给移动客户经理，大约本月中下旬开通，活动时间一年。</li>
  </ul>
</div>
<!-- 之前_scripts中内容在这 -->

<script>
$(function(){
  var fn_btn_submit = function() {
    var username = $('#username').val();
    var phone    = $('#phone').val();
    var inputs   = $(':input[name*="families"]');
    var submit_tag = true;
    inputs.each(function(){
      if ($(this).val() == '') {
        UIkit.notify('提交失败，信息填写不完整', {status:'warning'});
        submit_tag = false;
        return false;
      }
    });
    if (username == '') {
        UIkit.notify('姓名为空', {status:'warning'});
        return false;
    }
    return submit_tag;
  }

  var fn_btn_add = function() {
    var insert_html = '<div class="uk-form-row families_person">' +
      '<label class="uk-form-label" for="">开户姓名</label>' +
       '<div class="uk-form-controls">' +
         '<input type="text" value="" name="post[families][][name]" class="uk-width-1-1" placeholder="请输入开户姓名">' +
       '</div>' +
       '<label class="uk-form-label" for="">手机号码</label>' +
       '<div class="uk-form-controls">' +
         '<input type="text" value="" name="post[families][][id_number]" class="uk-width-1-1" style="margin-top:5px;" placeholder="请输入手机号">' +
       '</div>' +
       '<label class="uk-form-label" for="">活动类型</label>' +
       '<div class="uk-form-controls">' +
        '<span class="id-pre uk-width-1-1">' +
          '<select id="pre" name="post[families][][type]">' +
            '<option value="两个都参加">两个都参加</option>' +
            '<option value="每月1G流量">每月1G流量</option>' +
            '<option value="每月100分钟通话">每月100分钟通话</option>' +
          '</select>' +
         '</span>' +
      '</div>' +
    '</div>';
    $('.families-div').after(insert_html)
    $('.families_type').change(change_families_type);
  }


  var fn_btn_remove = function() {
    $('.families_person').first().remove();
  }

  $('#btn_submit').click(fn_btn_submit);
  $('#btn_add').click(fn_btn_add);
  $('#btn_remove').click(fn_btn_remove);

  var change_families_type = function() {
    if ($(this).val() == '成人') {
      $(this).parents().next().next().val('N/A');
    } else {
      $(this).parents().next().next().val('');
    }
  }
  $('.families_type').change(change_families_type);
});
</script>
<style type="text/css" media="screen">
  .tm-article-subtitle {
    padding-left: 6px;
    border-left: 3px solid #1FA2D6;
    font-size: 16px;
    line-height: 16px;
  }
</style>
